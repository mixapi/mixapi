[project]
name = "mixapi"
version = "0.1.0"
description = "Unified LLM gateway MVP"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.128.0",
  "httpx>=0.28.0",
  "jsonschema>=4.25.0",
  "pydantic>=2.12.0",
  "uvicorn>=0.40.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
